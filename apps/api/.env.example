DATABASE_URL=postgres://elite_reach:elite_reach_dev@localhost:5432/elite_reach
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
JWT_SECRET=change-me-in-production
PORT=4200

# The running Elite Escape OS platform's public API base URL — used to push
# qualified opportunities into the real CRM (POST /api/public/leads).
ELITE_ESCAPE_OS_URL=http://localhost:4100

# Connectors below are honestly reported as "disabled" until these are set.
# REDDIT_CLIENT_ID=
# REDDIT_CLIENT_SECRET=
# YOUTUBE_API_KEY=
